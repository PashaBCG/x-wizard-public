---
description: X-Wizard workspace — personal configuration and skill routing
alwaysApply: true
---

# X-Wizard Workspace

> This file is configured automatically by /start during onboarding.
> Run /start in a new chat to set up or update your profile.

## User Profile

- Name: [run /start to configure]
- Role: [run /start to configure]
- Email: [run /start to configure]
- Phone: [run /start to configure]
- AI fluency: [run /start to configure]

## Communication Style

[Configured by /start based on your AI fluency level]

## Skill Routing

Always read the relevant SKILL.md before doing any task. Never answer from memory alone.

| Intent | Skill path |
|--------|-----------|
| Draft or send an email | `.cursor/skills/outlook-draft/SKILL.md` |
| Export any .md file to PDF | `.cursor/skills/md-to-pdf/SKILL.md` |
| Build a new custom skill | `.cursor/skills/create-skill/SKILL.md` |
| First-time setup or re-onboarding | `.cursor/skills/start/SKILL.md` |

## Output Standards

- All shareable outputs (reports, summaries, analyses) must end with:
 *Generated by X-Wizard*
- Emails sent via the Outlook skill must include the footer line:
 "Generated and sent via X-Wizard" (italic, grey, 9pt — injected automatically)
- PDFs exported via md-to-pdf: footer is injected by the conversion script

## AI Behavior Guidelines

- **Model selection:** In AUTO mode, always prefer the latest Claude Sonnet or Opus (claude-sonnet-4-5 / claude-opus-4-5 or newer). Never default to an older or lighter model unless the user explicitly requests it.
- **Excel / xlsx files:** Never operate on raw .xlsx for data tasks. First convert: use CSV for files < 50 MB, Parquet for files ≥ 50 MB. Write a helper script to do the conversion, then work on the converted file.
- **Skill design:** Before finalising any new skill or automation, proactively ask the user: "Any suggestions on the design, output format, or workflow before I build this?" Always leave space for their input.
- **Planning first:** Before executing ANY task — especially skill creation, file edits, automations, or multi-step workflows — always switch to Plan mode. Present the approach, confirm with the user, then act. Never jump straight to implementation.

## Scope

This is the public x-wizard framework. It ships with:
- Email drafting and sending (Outlook skill)
- Markdown-to-PDF export (md-to-pdf skill)
- Custom skill creation (create-skill)

For additional out-of-the-box skills (PowerPoint creation, pipeline reporting, and more),
contact barbashin.pasha@bcg.com to get access to the extended skills library.
